"Included scalars"
scalar JSON

type Query {
  "Test query with a parameter"
  hello(name: String): String!

  "Fetch machine disks"
  disks: [Disk]!

  "Fetch directory contents"
  directory(path: String!): DirectoryResponse

  "Activate file default action"
  fileAction(path: String!): ConfirmationResponse

  "Fetch all pinned directories"
  allPinned: [Pinned]
}

type Mutation {
  "Add or Delete Pinned directory of path"
  togglePinned(path: String!): ConfirmationResponse
}
